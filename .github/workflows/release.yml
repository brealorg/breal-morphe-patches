name: Release

on:
  workflow_dispatch:

jobs:
  release:
    name: Release disabled pending Morphe workflow rewrite
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Release workflow is intentionally disabled."
          echo "Use scripts/prepare-release.py and the manual Morphe release path until this workflow is rewritten."
          exit 1
